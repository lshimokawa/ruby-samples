require 'rubygems'
require 'cucumber'
require 'cucumber/rake/task'

Cucumber::Rake::Task.new("bdd-cucumber-chicken-sexing") do |t|
  t.cucumber_opts = "bdd-cucumber-chicken-sexing/features --format pretty"
end